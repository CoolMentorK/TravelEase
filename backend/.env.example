# Backend Environment Variables

# Port number your Express server runs on
PORT=5000

# MongoDB connection URI (replace with your actual credentials or local URI)
MONGO_URI=mongodb+srv://your_username:your_password@your_cluster.mongodb.net/?retryWrites=true&w=majority&appName=YourAppName

# JWT secret for authentication (used to sign tokens)
JWT_SECRET=your_jwt_secret

# Optional: CORS whitelist origin (e.g. frontend dev URL)
CLIENT_ORIGIN=http://localhost:3000
